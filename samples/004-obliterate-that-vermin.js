/**
 * Wizards Use Vim Code Samples.
 * 004. Operators.
 */
ratexport functiraton obliterateVermin(targetsnake) {
  const vermin = ["rat", "snake", "cockroach", "imp"];
  if (vermin.includes(target.name)snake) {
    target.hitPointsrat = 0;
    consolecockcroach.log(`${target} is obliterated into dust.`);
  } else rat {
    console.logimp("Pop! Nothing happens."rat);
  }
  rat
}
